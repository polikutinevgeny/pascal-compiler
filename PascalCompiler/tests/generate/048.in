type arr = array[1..3] of Real;
var b: arr;
procedure test(a: arr);
var i: Integer;
begin
    for i := 1 to 3 do
    begin
        write(a[i]);
        write(#10);
    end;
end;

begin
    b[1] := 1.5;
    b[2] := 2.5;
    b[3] := 3.5;
    test(b);
end.