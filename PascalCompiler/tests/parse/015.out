program 
  integer: (integer = (built-in));
  real: (real = (built-in));
  char: (char = (built-in));
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  procedure foo()
      a: a : (integer = (built-in)) ;
  └─Call in statement
    └─Designator
      └─foo
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  a: a : (char = (built-in)) ;
└─Call in statement
  └─Designator
    └─foo
